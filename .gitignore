# 二进制与构建产物
/gateway
/bin/
*.exe

# 运行数据与本地配置(密钥不提交)
*.db
*.db-wal
*.db-shm
config.yaml
# 渠道密钥加密主密钥(自动生成,0600,不入库)
gateway.master.key

# 环境
.env
.env.*

# 前端依赖与构建产物(web-v2)
web-v2/node_modules/
web-v2/dist/
